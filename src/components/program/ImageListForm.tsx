type ImageListFormTypes = NonNullable<unknown>;

// eslint-disable-next-line no-empty-pattern
const ImageListForm = ({}: ImageListFormTypes) => {
  return <article></article>;
};
export { ImageListForm };
